[project]
name = "approval-service"
version = "1.0.0"
description = "Workspace-scoped content approval backend service"
requires-python = ">=3.11"

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 100
target-version = "py311"

